extends layouts/main-layout.pug

block content
    .chatPageContainer
        .chatTitleBar 
            label(for='userSearchTextbox') To: 
            #selectedUsers 
                input#userSearchTextbox(type='text', placeholder='Search user')
        .resultsContainer
        button#createChatButton(disabled) Create chat