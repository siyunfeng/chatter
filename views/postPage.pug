extends layouts/main-layout.pug

block content
    script. 
        const postId = '!{postId}'
    .postsContainter
    
    +createReplyModal(loggedInUser)

block scripts
    script(src='/js/postPage.js') 